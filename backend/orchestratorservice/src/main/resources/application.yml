spring:
  application:
    name: orchestrator-service
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    driver-class-name: org.postgresql.Driver

ffmpeg-path: C:\Projects\java_projects\speechcoach\backend\transcriptservice\bin\ffmpeg.exe

transcription-service:
  url: localhost:8081
intelligence-service:
  url: localhost:8080
server:
  port: 8084